{{- if .Values.pgTileserv.enabled }}
{{- if (not .Values.pgTileserv.auth.existingSecret ) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ird-mgb-hyfaa.fullname" . }}-pg-tileserv-secret
  labels:
    {{- include "ird-mgb-hyfaa.labels" . | nindent 4 }}
type: Opaque
data:
  DATABASE_URL: {{ .Values.pgTileserv.auth.databaseUrl | b64enc | quote }}
{{- end }}
{{- end }}
